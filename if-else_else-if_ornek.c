#include<stdio.h>
int main() {
	
	/*
	int vize1, vize2, final;
	float uniort, dersort;
	
	printf("1.Vize notunuzu giriniz:");
	scanf("%d",&vize1);
	printf("2.Vize notunuzu giriniz:");
	scanf("%d",&vize2);
	printf("Final notunuzu giriniz:");
	scanf("%d",&final);
	printf("Universite ortalamanizi giriniz:");
	scanf("%f",&uniort);
	
	dersort=(vize1*3/10.0 + vize2*3/10.0 + final*4/10.0);
	
	if (dersort >=90){
		printf("Harf Notunuz -AA ve Ders ortlamaniniz:%f\n", dersort);
		
	}
	else if (dersort >=85 && dersort < 90) {
		printf("Harf Notunuz -BA ve Ders ortlamaniniz:%f\n", dersort);
		
	}
	else if (dersort >=80 && dersort < 85){
		printf("Harf Notunuz -BB ve Ders ortlamaniniz:%f\n", dersort);
		
	}
	else if (dersort >=75 && dersort <80){
		printf("Harf Notunuz -CB ve Ders ortlamaniniz:%f\n", dersort);
		
	}
	else if (dersort >=70 && dersort <75){
		printf("Harf Notunuz -CC ve Ders ortlamaniniz:%f\n", dersort);
	}
	else if (dersort >=65 && dersort <70){
		printf("Harf Notunuz -DC ve Ders ortlamaniniz:%f\n", dersort);
	
	}
		
	else if (dersort >=60 && dersort <65){
		printf("Harf Notunuz -DD ve Ders ortlamaniniz:%f\n", dersort);
			if (uniort < 2.5){
			printf("dersi tekrar al");
		}
	}
	
	else{
		
		printf("Dersten kaldýnýz.\n");
		printf("dönemi tekrar oku");
	}
	
	Ders ortalamasý ve üni ortalamasý dahil harf notu hesaplayan program.
	*/
	
	return 0;
}
